# النسخ الاحتياطي والاستعادة والتصدير

ثلاثة أشياء مختلفة تُخلط كثيراً:

| | لمن | ما هو |
|---|---|---|
| **التصدير** | المؤسسة المشتركة | بياناتها بصيغةٍ تُقرأ خارج نهج (JSON و CSV) |
| **النسخة الاحتياطية** | مالك المنصة | لقطةٌ من قاعدة البيانات تُستعاد عند العطل |
| **الاستعادة** | مالك المنصة | إرجاع النظام إلى لقطةٍ سابقة |

---

## ١. التصدير — حقٌّ لا ميزة

من **شاشة الاشتراك ← بياناتك**، يُخرج المشرف أو المدير:

- **نسخة كاملة (JSON)**: المهارات، السياسات، مصادر المعرفة، حالات العمل،
  الموافقات، سجلّ التدقيق، حالات الاختبار، مقارنات الظل، والاشتراك والفواتير
  والدفعات.
- **دفاتر CSV**: الفواتير، الدفعات، سجلّ التدقيق، المهارات، حالات العمل.
  (وللمالك وحده: عمولات المسوّقين.)

وثلاثة قرارات في التصدير تستحق الذكر:

1. **متاحٌ حتى والكتابة مجمّدة.** منعُ التصدير عن متأخّرٍ عن السداد ابتزازٌ لا
   تحصيل. البيانات للمؤسسة، والخدمة هي المُباعة.
2. **لا يحمل سرّاً.** لا كلمات مرور ولا رموز جلسات ولا مفاتيح مزوّدين. من
   يأخذ نسخةً من بياناته لا يأخذ معها القدرة على انتحال حساباتها.
3. **يُسجَّل بمن فعله.** إخراج بيانات مؤسسة حدثٌ أمني بقدر ما هو خدمة، فيظهر
   في سجلّ التدقيق باسم من صدّر وبتاريخه.

> ملاحظة على CSV: كل خلية تبدأ بـ`=` أو `+` أو `-` أو `@` تُسبق بفاصلة عليا،
> فلا تُنفَّذ صيغةً عند فتح الملف في إكسل. وهذا فرقٌ أمني لا تجميلي.

---

## ٢. النسخ الاحتياطي

```bash
NAHJ_BACKUP_HOURS=24      # كل كم ساعة (0 = تعطيل الدوري؛ اليدوي يبقى)
NAHJ_BACKUP_KEEP=14       # كم نسخة تُستبقى — الأقدم يُحذف أولاً
NAHJ_BACKUP_DIR=          # المجلد (الافتراضي: backups/ بجوار القاعدة)
```

من **لوحة المالك ← النسخ الاحتياطي**: حالة النسخ، وآخر نسخة، وزرّ «انسخ الآن».

**لماذا `VACUUM INTO` لا نسخُ ملف؟** نسخُ ملف SQLite بينما يُكتب إليه يُنتج
ملفاً حجمه صحيح وبنيته تالفة، ولا يُكتشف ذلك إلا يوم يُحتاج — وهو اليوم الذي
لا يُقبل فيه الاكتشاف. `VACUUM INTO` يكتب قاعدةً متّسقة ومضغوطة وهي قيد
الاستعمال.

> **ضع مجلد النسخ على قرصٍ غير قرص القاعدة.** نسخةٌ بجوار الأصل تضيع معه.
> على خادم: قرصٌ آخر أو مزامنة إلى تخزين كائنات. وراجع أن النسخ تُكتب فعلاً
> بعد أول يوم — النسخة التي لم تُجرَّب ليست نسخة.

---

## ٣. الاستعادة

```bash
# ١. أوقف الخدمة
systemctl stop nahj          # أو أوقف الحاوية

# ٢. احتفظ بالحالة الحالية قبل أن تكتب فوقها
mv /var/nahj/nahj.sqlite /var/nahj/nahj.sqlite.before-restore

# ٣. ضع النسخة مكانها
cp /var/nahj/backups/nahj-2026-09-20T02-00-00-000Z.sqlite /var/nahj/nahj.sqlite

# ٤. احذف ملفات السجلّ المرافقة إن وُجدت (تخصّ القاعدة القديمة)
rm -f /var/nahj/nahj.sqlite-wal /var/nahj/nahj.sqlite-shm

# ٥. شغّل، ثم تأكد من الشاشة قبل أن تطمئن
systemctl start nahj
```

**بعد الاستعادة تأكد من ثلاثة** قبل أن تُعلن نجاحها: تسجيل الدخول يعمل،
وسجلّ التدقيق يُظهر أحدث حدث قبل العطل، والاشتراك والفواتير كما تتوقّع.

> ما بين وقت النسخة ووقت العطل يضيع. تقصير الفترة (`NAHJ_BACKUP_HOURS=6`)
> يُقلّل الفاقد ويزيد المساحة — القرار قرارك، لكن اتخذه قبل العطل لا بعده.
