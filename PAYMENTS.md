# بوابة الدفع — الربط والتشغيل

هذا الملف يُغلق آخر فجوة بين «النظام جاهز» و«النظام يُحصِّل». وهو مكتوبٌ ليُنفَّذ
خطوةً خطوة في أقل من ساعة، وآخره قائمةُ ما يجب أن تراه قبل أن تُحوّل أول دينار
حقيقي.

---

## ١. ماذا يفعل نهج بالضبط

عند إصدار فاتورة اشتراك، تستطيع المؤسسة المشتركة أن تضغط **«ادفع»** من شاشة
الاشتراك، فتُفتح صفحة الدفع عند المزوّد (كي نت، بطاقة). وحين يقع السداد:

1. يُرسل المزوّد إشعاراً (webhook) إلى نهج.
2. يتحقق نهج من **توقيع** الإشعار.
3. ثم **يسأل المزوّد بنفسه** عن حالة العملية ومبلغها — لا يُصدّق الإشعار.
4. يُطابق المبلغ والعملة بما أُنشئت به العملية.
5. عند التطابق تُسجَّل الدفعة على الفاتورة **مرةً واحدة** مهما تكرّر الإشعار،
   وتتحوّل الفاتورة إلى «مسدَّدة» وتُرفع حالة التجميد عن المؤسسة تلقائياً.

وما لا يفعله نهج: لا يلمس بيانات بطاقة ولا يخزّنها. صفحة الدفع مستضافةٌ عند
المزوّد، ونهج لا يرى إلا معرّف العملية ونتيجتها — وهذا ما يُبقي التزامك بـPCI
عند حدّه الأدنى.

**وبلا إعدادٍ مكتمل لا يظهر زرّ دفع أصلاً**، ويُقال على الشاشة إن السداد يتم
بتحويل ويُسجّله المالك بمرجعه. لا رابط يقود إلى صفحة لا تعمل.

---

## ٢. المزوّدون المدعومون

| المزوّد | الرمز | يناسب | ما يدعمه |
|---|---|---|---|
| ماي فاتورة | `myfatoorah` | الكويت والخليج | كي نت، بطاقات، أقساط |
| تاب | `tap` | الكويت والخليج | كي نت، بطاقات، Apple Pay |

كلاهما يدعم **الدينار الكويتي بثلاث منازل عشرية**، ونهج يُرسل المبالغ ويقرأها
بثلاث منازل. (أكثر الأخطاء كلفةً في تكاملات الدفع الخليجية أن يُفترض للدينار
منزلتان، فيُحصَّل عُشر المبلغ أو عشرة أضعافه.)

---

## ٣. متغيرات البيئة

```bash
NAHJ_PAYMENT_PROVIDER=myfatoorah        # myfatoorah | tap | manual
NAHJ_PAYMENT_API_KEY=<مفتاح المزوّد>
NAHJ_PAYMENT_WEBHOOK_SECRET=<سرّ التوقيع>
NAHJ_PUBLIC_URL=https://app.example.com  # عنوان النشر كما يراه المزوّد
NAHJ_PAYMENT_ENV=test                    # test | live
# NAHJ_PAYMENT_API_BASE=...              # اختياري: عنوان API غير القياسي
```

الثلاثة الأولى مع `NAHJ_PUBLIC_URL` **كلها** لازمة. ونصفُ إعدادٍ لا يُحصِّل
ديناراً — لذلك يرفض `verify-env` النشرَ عليه بدل أن يتركك تظنّ أنك ربطت:

```bash
npm run verify:env -- --strict
```

> `NAHJ_PUBLIC_URL` يجب أن يكون **https**: لا يُرسل أيٌّ من المزوّدَين إشعاراً
> إلى عنوان غير مشفّر.

---

## ٤. الربط — ماي فاتورة

1. من لوحة ماي فاتورة: **API Key** → أنشئ مفتاحاً لبيئة الاختبار
   (`apitest.myfatoorah.com`) وضعه في `NAHJ_PAYMENT_API_KEY`.
2. ولّد سرّ توقيع عشوائياً وضعه في الطرفين — عند المزوّد وفي
   `NAHJ_PAYMENT_WEBHOOK_SECRET`:
   ```bash
   openssl rand -hex 32
   ```
3. سجّل عنوان الإشعار عند المزوّد:
   ```
   https://<عنوانك>/api/payments/webhook/myfatoorah
   ```
4. شغّل الخدمة، وافتح **لوحة المالك ← بوابة الدفع**: يجب أن تقرأ
   «ماي فاتورة — مربوطة (بيئة اختبار)».
5. أصدر فاتورة اختبار من لوحة المالك، ثم ادفعها من شاشة الاشتراك ببطاقة
   الاختبار التي يعطيها المزوّد.

## ٥. الربط — تاب

1. من لوحة تاب: **Secret Key** (يبدأ بـ`sk_test_`) في `NAHJ_PAYMENT_API_KEY`.
2. سرّ التوقيع من إعدادات Webhooks، وهو نفسه الذي تُحسب به `hashstring`.
3. عنوان الإشعار:
   ```
   https://<عنوانك>/api/payments/webhook/tap
   ```
4. بقيّة الخطوات كما أعلاه.

---

## ٦. الانتقال إلى البيئة الحيّة

بعد أن تنجح دورة اختبارٍ كاملة (دفع → إشعار → فاتورة مسدَّدة):

1. بدّل المفتاح إلى مفتاح الإنتاج.
2. `NAHJ_PAYMENT_ENV=live`.
3. أعد تسجيل عنوان الإشعار في لوحة الإنتاج عند المزوّد.
4. `npm run verify:env -- --strict` — يجب أن يخرج بلا موانع.
5. أعد تشغيل الخدمة، ثم **ادفع فاتورةً حقيقيةً صغيرة بنفسك** وتأكد من وصولها
   إلى حسابك البنكي. لا تبدأ التحصيل من العملاء قبل هذه الخطوة.

---

## ٧. ما تراه في لوحة المالك

**بوابة الدفع** تعرض:

- حالة الربط والبيئة (حيّة/اختبار) — مقروءةً من الإعداد لا مكتوبةً في الشيفرة.
- عنوانَي الإشعار والعودة لتنسخهما إلى لوحة المزوّد.
- **عمليات تحتاج تدخّلك**: كل عملية حُصِّل فيها مبلغ يخالف الفاتورة، أو
  حُصِّلت ولم تُسجَّل. وهذه لا تُسجَّل تلقائياً أبداً — المال الذي لا يطابق
  يُراجَع بشراً.
- دفتر العمليات الأخير بمرجع المزوّد لكل واحدة، ليُطابَق مع كشف الحساب البنكي.

---

## ٨. لماذا بُني هكذا

خمس قواعد، كلٌّ منها مكتوبةٌ بخطأ نظامٍ وقع فعلاً:

| القاعدة | ما تمنعه |
|---|---|
| لا تُصدَّق الإشعارات — يُسأل المزوّد | من يسجّل الدفع من جسم الطلب يُسدَّد عنده بطلبٍ مُلفَّق |
| التسوية ذرّية (`UPDATE ... WHERE status <> 'paid'`) | إشعارٌ مكرّر يُسجَّل دفعتين على فاتورة واحدة |
| المبلغ والعملة يُطابَقان قبل أي كتابة | خطأ منزلةٍ عشرية يُسجَّل كسدادٍ كامل |
| المبلغ يُشتق في الخادم من المتبقّي | لا يدفع أحدٌ ما يشاء وتُعدّ الفاتورة مسدّدة |
| مسار الدفع خارج حارس الاشتراك | مؤسسةٌ جُمِّدت لعدم السداد تُمنع من السداد — قفلٌ لا مخرج منه |

والتحقق من هذه الخمس آليّ: `server/payments.test.ts`.

---

## ٩. عند العطل

| العَرَض | السبب الغالب |
|---|---|
| لا يظهر زرّ الدفع | الإعداد ناقص — افتح لوحة المالك، القسم يقول ما ينقص بالاسم |
| «توقيع الإشعار غير صالح» | السرّ مختلف بين الطرفين، أو وسيطٌ يُعيد بناء جسم الطلب |
| الدفع تمّ والفاتورة لم تتحدّث | عنوان الإشعار غير مسجَّل عند المزوّد، أو لا يصله https |
| عملية في «تحتاج مراجعة» | المبلغ المحصَّل يخالف الفاتورة — راجع كشف المزوّد قبل أي تسجيل يدوي |

عند كل حالة، سجلّ الترخيص (لوحة المالك ← سجلّ الترخيص) يحمل الحدث بتاريخه
ومرجعه: `payment.intent.created`، `payment.intent.mismatch`،
`payment.settlement.failed`.
