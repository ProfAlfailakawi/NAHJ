# الإشعارات

نهج يعمل بلا بريد، ويُباع بلا بريد. وربطُ البريد يجعل التحصيل أسهل ويمنع
مفاجأةً واحدة بعينها: مؤسسةٌ تُجمَّد كتابتها يوم الاثنين لأن أحداً لم يخبرها
أن اشتراكها ينتهي الأحد — وهي لا تلوم نفسها، تلوم المنتج.

---

## ما يُرسل

| الحدث | إلى | متى |
|---|---|---|
| صدور فاتورة | مشرفو المؤسسة ومديروها | لحظة الإصدار |
| وصول سداد | المؤسسة والمالك | لحظة تسجيل الدفعة |
| قرب التجديد | المؤسسة والمالك | قبل ١٤ و٧ و٣ ويوم واحد |
| تجمّد الكتابة | المؤسسة والمالك | عند وقوعه |

## ما لا يُرسل أبداً

**كلمة مرور، أو رابط تعيين كلمة مرور، أو رمز دخول.** البريد قناةٌ تُخزَّن
وتُعاد توجيهاً وتُقرأ من هاتفٍ ضائع. المشرف يسلّم الكلمة المؤقتة بيده — أضعف
راحةً وأقوى أماناً، وهو ما يفعله نهج منذ أول يوم.

---

## الربط

```bash
NAHJ_MAIL_PROVIDER=resend        # resend | sendgrid | webhook
NAHJ_MAIL_API_KEY=<مفتاح المزوّد>
NAHJ_MAIL_FROM=nahj@example.com  # نطاق موثّق عند المزوّد
NAHJ_MAIL_INTERVAL_MINUTES=15
```

**أو بلا بريد أصلاً** — عنوانٌ يستقبل الإشعار JSON فتوصّله المؤسسة بنظامها
(واتساب، سلاك، بوابة داخلية):

```bash
NAHJ_MAIL_PROVIDER=webhook
NAHJ_MAIL_WEBHOOK_URL=https://hooks.example.com/nahj
```

جسم الطلب: `{ kind, to, subject, body, at }`.

ثم `npm run verify:env` — يرفض نصف الإعداد بدل أن يتركك تظنّ أنك ربطت.

---

## أربع قواعد في التنفيذ

1. **بلا مزوّد لا يُدَّعى إرسال.** الحدث يُسجَّل ويُعلَّم «لم يُرسل — لا مزوّد»،
   فيراه المالك في لوحته ويعرف ما فاته حين يربط البريد لاحقاً. وواجهةٌ تقول
   «أُرسل تنبيه» بلا بريد مضبوط أسوأ من صمتٍ صريح.
2. **لا يُرسَل مرّتين.** لكل إشعارٍ مفتاحُ تفرّدٍ من الحدث وموضوعه، وقيدٌ فريد
   في القاعدة. فتذكيرُ التجديد يُحسب كل ربع ساعة ويصل مرةً واحدة لكل عتبة.
3. **لا يُعطّل الطلب.** الإرسال في عاملٍ دوري لا في مسار الطلب: بطءُ مزوّد
   بريد لا يجوز أن يُبطئ تسجيل دفعة أو فتح شاشة.
4. **الفشل يُسجَّل ويُعاد بحدّ.** خمس محاولات ثم يُترك فاشلاً ظاهراً بسببه —
   ومحاولةٌ لا تنتهي تُخفي عطلاً دائماً في الإعداد.

## المتابعة

**لوحة المالك ← الإشعارات**: كم أُرسل، وكم في الطابور، وكم تُرك لغياب مزوّد،
وكم فشل — ولماذا فشل، بنصّ ردّ المزوّد نفسه.
