# قائمة ما قبل البيع

ما في هذا الملف ليس وعداً تسويقياً — هو جردٌ صريح لما يعمل، ولما يحتاج قراراً
منك، ولما ما زال محاكاةً معلنة. تُقرأ قبل أول عرضٍ على عميل، وتُنفَّذ خطواتها
الستّ قبل أول فاتورة حقيقية.

---

## أ. ما هو جاهز اليوم

| المجال | الحالة |
|---|---|
| التعلّم من العمل (Teach) | يُركّب المهارة ممّا سُجِّل فعلاً، بلا مفتاح نموذج لغوي ولا قوالب |
| الحوكمة | سياسات، موافقات، مستويات استقلالية L0–L6، سجلّ تدقيق لا يُعدَّل |
| بوابة الاستقلالية | لا تُرفع صلاحية مهارة إلا بعد تدرّبٍ حقيقي؛ الحالة التي يتعذّر تقييمها ترسب |
| القياس | كل رقم مشتقّ من العمل، ويحمل أساسه وحجم عيّنته — لا ثوابت |
| الاشتراك والفوترة | باقات، دورات، تناسب زمني، مهلة سماح، تجميد الكتابة دون القراءة |
| **التحصيل الإلكتروني** | **ماي فاتورة / تاب — راجع `PAYMENTS.md`** |
| المسوّقون والعمولات | لوحة لكل مسوّق، عمولة نسبةً أو مبلغاً، دفتر استحقاقٍ ودفع عند المالك |
| القطاعات | حزم جاهزة: تعليم، عيادات، محاماة، عقار، تجزئة، لوجستيات |
| الأمان | جلسات مُجزَّأة، CSRF، أدوار، عزل المسوّق، بيئة تجريبية معزولة تماماً |
| اللغة | عربيةٌ أولاً بواجهة RTL كاملة، وإنجليزية للعرض |
| **التصدير والنسخ** | تصدير JSON و CSV للمؤسسة، ونسخٌ احتياطي دوري بـ`VACUUM INTO` — `BACKUP.md` |
| **الإشعارات** | فاتورة، سداد، قرب تجديد، تجميد — وتعمل بلا بريد وتقول ذلك — `NOTIFICATIONS.md` |
| **صفحة البيع** | `/pricing` علنية، تُبنى من الباقات نفسها التي يُفوتَر بها |
| **صفحة الهبوط** | `/` لكل زائر بلا جلسة: ما نهج، ولمن، والقطاعات، ونموذج «اطلب عرضاً» |
| **طلبات العرض** | تُحفظ في القاعدة وتظهر في لوحة المالك ← طلبات العرض، وتُرسل إلى بريدك إن رُبط البريد |
| **إعداد المؤسسة** | أول دخولٍ لمشرفها: اسمها وقطاعها ← بدايةٌ نظيفة (قوالب مسوّدة، لا بيانات نموذجية) |
| **العرض التجريبي — لك وحدك** | لا يظهر للزوار ولا لموظفي المؤسسة. روابطه في لوحة المالك ← روابط العرض: `/try/clinic` · `/try/law` · `/try/retail` · `/try/logistics` · `/try/realestate` · `/try/education` |
| **الشروط والخصوصية** | `/terms` و`/privacy` — نصّ أوّلي يُراجَع قانونياً قبل البيع (انظر هـ) |

## ب. ما هو محاكاة — ومعلنٌ على الشاشة

موصلات الأنظمة الخارجية (نظام معلومات الطلاب، التقويم، خزينة الوثائق، سجلّ
التواصل) **لا تُخرج طلب شبكة واحداً**. وهي معروضةٌ في شاشة الربط بوسم «محاكاة»
صريح، مع تنبيه أن أرقامها للعرض.

هذا مقصود ومُعلن: لا يُباع ما ليس مبنياً. وبناء أول وصلةٍ حقيقية يحتاج قرارك
بأي نظامٍ نبدأ ومَن يملك مفاتيحه عند العميل. **بوابة الدفع هي الوصلة الحقيقية
الأولى، وهي مبنيّة.**

---

## ج. الخطوات قبل أول دينار — بالترتيب

1. **الخادم.** نفّذ `docs/deploy-nahj.md` مرةً واحدة (هوية النشر في GitHub). بعدها
   كل دمجٍ إلى main يُنشر وحده على خادمٍ بقرصٍ دائم — والقرص لا يُمسّ في أي نشر.
   تحقّق: آخر تشغيل في تبويب Actions فيه خطوة «النشر عبر سكربت المستودع» **منفّذة**
   لا «متخطّاة».
2. **النطاق.** `docs/domain.md`: سجلّ `A` ← `NAHJ_DOMAIN` في GitHub ← تشغيل الورك-فلو.
   تصدر الشهادة ويصير العنوان العلني نطاقك تلقائياً.
3. **حساب المالك.** افتح `https://<نطاقك>/app` وأنشئ حسابك من شاشة التهيئة. أنت
   المالك تلقائياً (أقدم حساب). ولوحتك متاحة فوراً من «افتح لوحة المالك أولاً»
   دون إعداد مؤسسة.
4. **إعدادات الخادم.** من Cloud Shell:
   `bash scripts/server-env.sh set NAHJ_ADMIN_NAME="..." NAHJ_CONTACT_EMAIL=... NAHJ_LEGAL_NAME="..."`
   (والواتساب اختياري). لا شيء منها في GitHub.
5. **الباقات والأسعار.** لوحة المالك ← الباقات: راجع الأسعار الثلاثة (شهري،
   ربع سنوي، سنوي) ورسوم التأسيس والحدود قبل أن يراها أي عميل.
6. **بوابة الدفع.** `PAYMENTS.md` على بيئة الاختبار، والمفاتيح بالأداة نفسها
   (`server-env.sh set NAHJ_PAYMENT_...`)، ثم انتقل إلى الحيّة.
7. **الفحص.** `bash scripts/server-env.sh check` — على الخادم نفسه، ويجب أن يخرج
   بلا موانع.
8. **دورة حقيقية صغيرة.** أصدر فاتورةً بقيمة زهيدة، ادفعها بنفسك، وتأكد من
   وصول المبلغ إلى حسابك البنكي ومن تحوّل الفاتورة إلى «مسدَّدة» تلقائياً.

**وأول عميل:** خادمٌ له (`docs/deploy-nahj.md` ← «خوادم العملاء»)، بـ
`NAHJ_MARKETING=off` فلا صفحة تسويق عنده ولا عرض. أول دخولٍ لمشرفه يفتح «أعِدّ
مؤسستك». وسطرُه في `NAHJ_CUSTOMERS` يُبقيه محدَّثاً مع كل دمج.

**واختيارية تُحسّن التشغيل ولا تمنع البيع** (كلها بـ `server-env.sh set`):

- **البريد** (`NAHJ_MAIL_PROVIDER` ومعه المفتاح والمُرسِل) — طلبات العرض تصل بريدك،
  وتذكيرُ التجديد قبل التجميد يمنع أكثر الشكاوى تكراراً. `NOTIFICATIONS.md` و`docs/domain.md` §٦.
- **الذكاء** (`GEMINI_API_KEY`) — بدونه تعمل المحادثة بردودٍ حتمية أبسط.
- **النسخ الاحتياطي على قرصٍ آخر** (`NAHJ_BACKUP_DIR`) — نسخةٌ بجوار الأصل تضيع
  معه. راجع `BACKUP.md`.
- **روابط العرض لكل قطاع** — من لوحة المالك: افتح رابط قطاع العميل في الاجتماع،
  أو أرسله له. ولإيقاف العرض كلياً في نشرٍ بعينه: `NAHJ_DEMO_ENABLED=false`.
- **صفحة الأسعار** `/pricing` — أرسل رابطها بدل صورة جدول: تتحدّث تلقائياً مع
  كل تغيير سعرٍ في لوحتك.

---

## د. ما تقوله للعميل بصدق

- «يتعلّم من موظفيكم وهم يعملون، ولا يحتاج منكم إعداداً مسبقاً.» ✅
- «لا ينفّذ إجراءً إلا بصلاحيةٍ استحقّها في التدرّب، وكل إجراء له سطرٌ في
  السجلّ لا يُعدَّل.» ✅
- «يعمل بالعربية كلغةٍ أولى.» ✅
- «يتكامل مع أنظمتكم القائمة.» ⚠️ ليس بعد — قُلها كخارطة طريقٍ لا كقدرةٍ قائمة.

الفرق بين الثلاث الأولى والرابعة هو الفرق بين عميلٍ يجدّد وعميلٍ يشعر أنه
خُدع. ونهج مبنيٌّ على أن يُقال هذا الفرق قبل التوقيع لا بعده.

---

## هـ. ما تحتاجه قانونياً (خارج البرنامج)

> صفحتا `/terms` و`/privacy` مكتوبتان بصدق عمّا يفعله البرنامج (الكوكيز، والمعالجون
> الاختياريون Gemini وFirebase، ومزوّد الدفع)، لكنهما **ليستا مراجعتين من محامٍ**.
> راجعهما قبل أول عقد.

هذه ليست شيفرة، لكنها تمنع البيع إن غابت:

- عقد اشتراك واضح المدّة والتجديد والإنهاء.
- سياسة خصوصية ومعالجة بيانات — نهج يخزّن بيانات عمل المؤسسة.
- سجل تجاري وحساب تاجر لدى مزوّد الدفع (وهو شرط إصدار مفاتيح الإنتاج).
- اتفاقٌ مكتوب مع كل مسوّق: النسبة أو المبلغ، والمدّة، ومتى يُستحقّ. (الدفتر
  في نهج يُنفّذ الاتفاق، ولا يُغني عنه.)
