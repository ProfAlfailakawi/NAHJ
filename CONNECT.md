# نهج — دليل الربط

> ## ✅ قاعدة البيانات مقفلة — أُنجز
>
> كانت قواعد `nahj-a27a4` المُطبَّقة `allow read, write: if true`، ومعرّف المشروع
> ومفتاح الويب منشوران في هذا المستودع: أي شخص على الإنترنت كان يقرأ القاعدة أو
> يمحوها. ملف `firestore.rules` كان يمنع كل وصول، لكنه **لم يُنشر قط** — لم يكن في
> المستودع `firebase.json` أصلاً، فلم تكن هناك طريقة لنشره.
>
> أُضيف `firebase.json` و`.firebaserc`، ونُشرت القواعد بتاريخ ٢٠٢٦-٠٩-١٧:
>
> ```
> ✔  cloud.firestore: released rules firestore.rules to cloud.firestore
> ```
>
> **مُتحقَّق منه عملياً:** المزامنة نفسها التي كتبت ٣٢ مستنداً بنجاح قبل النشر
> تُرفض الآن بـ `PERMISSION_DENIED` في المستندات الاثنين والثلاثين كلها. هذا هو
> الدليل، لا رسالة النشر.
>
> لإعادة نشرها بعد أي تعديل على `firestore.rules`:
>
> ```bash
> npx firebase-tools deploy --only firestore:rules
> ```
>
> المزامنة إلى Firestore مقفلة عمداً منذ ذلك: تستعمل SDK العميل، والقواعد تمنع
> العملاء. زر «مزامنة» في شاشة الربط يُظهر الفشل صراحةً بالسبب. إعادة تفعيل
> المرآة تحتاج نقل الخادم إلى Admin SDK بحساب خدمة.

نهج لا يحتاج مزوّد دفع ولا بريد. إعداده كله يدور حول **سؤال واحد**، والباقي اختياري.

```bash
npm run verify:env
```

---

## السؤال الوحيد الذي يقرّر: هل القرص دائم؟

كل حالة المنصة — المهارات المرقّاة، الموافقات، سجل الأثر، الحسابات والجلسات — في
ملف SQLite واحد. على قرص مؤقت يُمحى مع كل نشر، **فيعود نهج نموذج عرض مهما فعلنا
في الكود**.

```
NAHJ_DATABASE_PATH=/var/nahj/nahj.sqlite
```

### الطريق الجاهز

```bash
export PROJECT_ID=your-project-id
bash scripts/deploy-vm.sh
```

ينشئ قرصاً دائماً وخادماً ويبني الصورة وينشرها، ويطبع العنوان. شغّله مرة أولى
للإنشاء، وبعد كل تحديث للنشر. **لا يحذف القرص ولا يمسّ بياناته أبداً.**

### لماذا VM لا Cloud Run

| الخيار | الحكم |
|---|---|
| VM + قرص دائم | ✅ WAL يعمل، قفل ملفات صحيح، ~٧$ شهرياً. هذا ما يفعله السكربت |
| Cloud Run + Filestore (NFS) | ⚠ يعمل بلا WAL، لكن أرخص باقة تتجاوز ٢٠٠$ شهرياً |
| Cloud Run + GCS FUSE | ❌ لا قفل ملفات حقيقياً. خطر تلف القاعدة |
| Cloud Run بلا وحدة تخزين | ❌ القرص مؤقت. تضيع الحالة مع كل نشر |

WAL يحتاج ذاكرة مشتركة بين العمليات، وهي غير متاحة على أنظمة الملفات الشبكية.
هناك لا يفشل SQLite بصوت مرتفع — يتجاهل الطلب. فالخادم يقرأ الوضع الفعلي بعد
ضبطه، ويتراجع إلى TRUNCATE مع تحذير صريح في السجل بدل افتراض أن WAL سرى.

⚠ **نسخة واحدة فقط من الخدمة.** SQLite لا يُشارَك بين نسخ متوازية. الـ VM نسخة
واحدة بطبيعته؛ على Cloud Run اضبط `--max-instances=1`.

**تحقّق منه بنفسك — لا يمكن إثباته من داخل العملية:** غيّر شيئاً (مثل مفتاح إيقاف
مهارة)، أعد النشر، ثم افتح التطبيق. إن بقي التغيير فالقرص دائم. إن عاد كل شيء
للبذرة فهو ليس كذلك.

---

## أول حساب — لا تحتاج ضبط شيء

شغّل البرنامج وافتحه في المتصفح. ستُعرض شاشة **«أنشئ حساب المشغّل الأول»**
تلقائياً ما دام لا يوجد أي حساب، وتُقفل نهائياً بمجرد إنشائه.

كلمة المرور: ١٢ محرفاً على الأقل، حروف وأرقام.

للتهيئة الآلية بلا تدخّل بشري فقط (نشر أوتوماتيكي):

```
NAHJ_ADMIN_EMAIL=you@example.com
NAHJ_ADMIN_PASSWORD=<12 محرفاً على الأقل>
```

ضبط واحد منهما دون الآخر يُتجاهلان معاً، وتُعرض شاشة التهيئة بدلاً منهما.

---

## اختيارية

| المتغيّر | الأثر |
|---|---|
| `GEMINI_API_KEY` | بدونه يعمل التوليف والمحاكاة بمنطقهما الحتمي — لا تعطّل |
| `NAHJ_DEMO_ENABLED=false` | يُقفل البيئة التجريبية في هذا النشر |

البيئة التجريبية مفعّلة افتراضياً: يدخلها الزائر من شاشة الدخول بلا حساب، فيحصل
على نسخة اصطناعية معزولة في الذاكرة. لا تُقرأ ولا تُكتب أي بيانات مؤسسة، ولا شيء
منها يصل القرص.

---

## قبل الإطلاق

```bash
npm run verify:env      # 0 مانع
npm test                # 16/16
npm run build
```

ثم بيدك على البيئة المنشورة:

1. افتح التطبيق وأنشئ حساب المشغّل.
2. غيّر شيئاً (رقِّ مهارة أو أوقفها).
3. **أعد النشر**، ثم تأكد أن التغيير باقٍ.

الخطوة ٣ هي الاختبار الوحيد الذي يثبت أن نهج منتج لا نموذج عرض.

---

## ما لا يوجد بعد

لا تسجيل ذاتي ولا استعادة كلمة مرور. إدارة المستخدمين موجودة: شاشة «الحسابات»
تُنشئ حسابات الفريق وتُعطّلها وتُبدّل الأدوار وتضبط كلمات المرور.

المزامنة إلى Firestore مقفلة (انظر أعلى الملف). لا شهادة TLS في سكربت النشر —
ضعها بنفسك قبل أي استعمال حقيقي.
