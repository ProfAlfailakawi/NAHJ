# نهج — دليل الربط

نهج لا يحتاج مزوّد دفع ولا بريد. إعداده كله يدور حول **سؤال واحد**، والباقي اختياري.

```bash
npm run verify:env
```

---

## السؤال الوحيد الذي يقرّر: هل القرص دائم؟

كل حالة المنصة — المهارات المرقّاة، الموافقات، سجل الأثر، الحسابات والجلسات — في
ملف SQLite واحد. على قرص مؤقت يُمحى مع كل نشر، **فيعود نهج نموذج عرض مهما فعلنا
في الكود**.

```
NAHJ_DATABASE_PATH=/var/nahj/nahj.sqlite
```

| البيئة | ما تفعله |
|---|---|
| خادم / VM | أي مسار على قرص دائم. انتهى الأمر |
| Cloud Run | اربط وحدة تخزين دائمة (Cloud Storage FUSE أو Filestore) ووجّه المسار إليها |
| Cloud Run بلا وحدة تخزين | ❌ لا تفعل. القرص مؤقت وستضيع الحالة مع كل نشر |

⚠ **نسخة واحدة فقط من الخدمة.** SQLite لا يُشارَك بين نسخ متوازية: اضبط
`--max-instances=1`، وإلا تسابقت النسخ على الملف نفسه.

**تحقّق منه بنفسك — لا يمكن إثباته من داخل العملية:** غيّر شيئاً (مثل مفتاح إيقاف
مهارة)، أعد النشر، ثم افتح التطبيق. إن بقي التغيير فالقرص دائم. إن عاد كل شيء
للبذرة فهو ليس كذلك.

---

## أول حساب — لا تحتاج ضبط شيء

شغّل البرنامج وافتحه في المتصفح. ستُعرض شاشة **«أنشئ حساب المشغّل الأول»**
تلقائياً ما دام لا يوجد أي حساب، وتُقفل نهائياً بمجرد إنشائه.

كلمة المرور: ١٢ محرفاً على الأقل، حروف وأرقام.

للتهيئة الآلية بلا تدخّل بشري فقط (نشر أوتوماتيكي):

```
NAHJ_ADMIN_EMAIL=you@example.com
NAHJ_ADMIN_PASSWORD=<12 محرفاً على الأقل>
```

ضبط واحد منهما دون الآخر يُتجاهلان معاً، وتُعرض شاشة التهيئة بدلاً منهما.

---

## اختيارية

| المتغيّر | الأثر |
|---|---|
| `GEMINI_API_KEY` | بدونه يعمل التوليف والمحاكاة بمنطقهما الحتمي — لا تعطّل |
| `NAHJ_DEMO_ENABLED=false` | يُقفل البيئة التجريبية في هذا النشر |

البيئة التجريبية مفعّلة افتراضياً: يدخلها الزائر من شاشة الدخول بلا حساب، فيحصل
على نسخة اصطناعية معزولة في الذاكرة. لا تُقرأ ولا تُكتب أي بيانات مؤسسة، ولا شيء
منها يصل القرص.

---

## قبل الإطلاق

```bash
npm run verify:env      # 0 مانع
npm test                # 11/11
npm run build
```

ثم بيدك على البيئة المنشورة:

1. افتح التطبيق وأنشئ حساب المشغّل.
2. غيّر شيئاً (رقِّ مهارة أو أوقفها).
3. **أعد النشر**، ثم تأكد أن التغيير باقٍ.

الخطوة ٣ هي الاختبار الوحيد الذي يثبت أن نهج منتج لا نموذج عرض.

---

## ما لا يوجد بعد

لا تسجيل ذاتي، ولا استعادة كلمة مرور، ولا واجهة لإدارة المستخدمين. بعد المشرف
الأول، الحسابات تُنشأ يدوياً. إن احتجت فريقاً يعمل على المنصة، هذه أول ميزة ستطلبها.
