# نهج — القياس والصدق العددي

> القاعدة: **كل رقم يُشتق من المخزن. وما لا يُشتق يُعلن أنه غير مقيس — لا يُخترع.**

---

## 1. لماذا هذا ملفٌّ قائم بذاته

كانت شاشتا الأثر والحوكمة تعرضان ثوابت مكتوبة في الشيفرة:

| الرقم المعروض | مصدره الحقيقي |
|---|---|
| 412 مهمة مكتملة | مكتوب في `routes.ts` |
| 84.5 ساعة مستعادة | مكتوب |
| 78.4% أتمتة · 94.2% تطابق ظل | مكتوبة |
| 143 عملية معروفة · 37 غير موثّقة | مكتوبة في `TodayView` |
| **0 تجاوزات** | **مكتوب** |
| حلقات السياسات 92% · 84% · 100% | مكتوبة في `ControlView` |
| 76% من العمل معروف · 168 ملاحظة | مكتوبة في `LearnView` |

وأخطرها «0 تجاوزات»: رقمٌ يدّعي إثباتاً لم يجرِ، على الشاشة التي يُفترض أنها
تُثبت أن الحوكمة تعمل.

المشكلة ليست أن رقماً خاطئ. المشكلة أن نهج **يبيع القياس والحوكمة تحديداً**.
مشترٍ يكتشف أن الصفر ثابتٌ لا يتغيّر مهما فعل، لن يصدّق بعدها سجلّ التدقيق ولا
نسبة المطابقة ولا شيئاً على الشاشة. **الرقم المخترع في منتج قياسٍ يُفسد كل
الأرقام الصادقة من حوله.**

---

## 2. المحرّك — `server/engine/metricsEngine.ts`

### كل مقياس يحمل أساسه

```ts
interface Measure {
  value: number | null;   // null = غير مقيس، لا صفر
  basis: string;          // تعريف المقياس بلغة تُقرأ
  sampleSize: number;     // عدد السجلات التي اشتُقّ منها
}
```

رقمٌ بلا أساس لا يُراجَع: «94%» من ماذا، وعلى كم حالة؟ الواجهة تعرض الأساس خلف
أيقونة، فلا يزاحم الرقم ولا يغيب عمّن يسأل.

### الترجيح بالاستعمال

نسب النجاح والاستلام والمدّة **مرجَّحة بعدد مرات التنفيذ لا بعدد المهارات**.
مهارة نُفِّذت 900 مرة بنجاح 100% وأخرى نُفِّذت 100 مرة بنجاح 50%: المتوسط البسيط
75% — وهو وصفٌ كاذب لما يحدث في المؤسسة. المرجَّح 95%.

### `null` لا صفر

بلا تنفيذ لا تُخترع نسبة. الصفر هنا يعني «نجاحنا صفر»، وهو ادّعاء آخر. الشاشة
تعرض `—`.

### المنحنى يمتنع معلناً

نشاط الأسبوع يُشتق من طوابع سجلّ التدقيق. حين لا يكفي التاريخ (يوم واحد أو لا
سجلّ) يُعلن السبب ولا يُرسم شيء. منحنى جميل على مؤسسة لم تبدأ بعد هو أول كذبة
يراها المشتري.

### التجاوزات تُشتق بالمطابقة

```
تجاوز = إجراء عالي الخطورة + نفّذه الذكاء + نجح + لا يقابله طلب موافقة معتمد
```

فصفرُه يعني **أننا بحثنا فلم نجد**، لا أننا كتبنا صفراً. وحين يقع تجاوز يظهر.

### ما لا يُعرف يبقى غير معروف

«العمليات غير الموثّقة» غير قابلة للمعرفة بطبيعتها: النظام لا يعلم ما لم يُعرض
عليه قطّ. تبقى `null` وتقول الشاشة «غير مقيس — يحتاج اكتشافاً من الموصلات» بدل
الرقم 37.

---

## 3. عيبان كشفهما الاختبار أثناء البناء

**١. `\b` لا ينطبق على الحروف العربية.** حدود الكلمة في تعابير جافاسكربت النمطية
تعمل على `\w` وحدها، والحروف العربية ليست منها. فكان `/\bم\b/` لا يطابق شيئاً
إطلاقاً، وتُقرأ كل ساعة مسائية صباحيةً: «02:10 م» تصير الثانية فجراً. الحلّ
مطابقة الرمز محاطاً بفراغ أو فاصلة أو طرف النصّ — وهكذا لا تُلتقط «م» من «أمس».

**٢. عُرفان مختلفان لأسماء الإجراءات.** سجلّ التدقيق يكتب `ISSUE_REFUND`
وطلبات الموافقة تكتب `issueRefund`. المطابقة بالحروف الصغيرة وحدها لا تجمعهما
أبداً، فكان **كل إجراءٍ محكومٍ بموافقة يُحتسب تجاوزاً** — إنذارٌ كاذب دائم على
الشاشة التي يُفترض أن تُطمئن. الحلّ تطبيعٌ يُسقط كل ما ليس حرفاً أو رقماً.

---

## 4. الحراسة

`server/metrics.test.ts` — 20 اختباراً على الاشتقاق نفسه، ومعها حارسان يقرآن
الشيفرة:

- لا ثابت من الثوابت القديمة عاد إلى `/analytics` أو `/today`.
- لا شاشة تكتب `value="123"` أو `progress={76}` من جديد.

وكائن الارتداد في الواجهة كان نسخةً ثانية من الأرقام نفسها — أي أن فشل الطلب
كان يُظهر أرقاماً جميلة بدل أن يقول إنه فشل. صار فارغاً صراحةً، والشاشة تعرض
«لا قياس بعد».
