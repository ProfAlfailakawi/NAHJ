# نهج — الاشتراك والترخيص (Billing & Licensing)

طبقةٌ تفصل **مالك المنصة** عن **المؤسسة المشترية**، وتجعل للنظام مدّةً وسعراً
وحدوداً بدل أن يكون مفتوحاً بلا عقد.

---

## 1. الدوران والفصل بينهما

| الدور | من هو | ما يملكه |
|---|---|---|
| `owner` | مالك المنصة — من باعها ونشرها | الباقات والتسعير، بدء الاشتراك وتمديده وتجديده ونقله وإنهاؤه، إصدار الفواتير، تسجيل الدفعات، أرقام الإيراد |
| `admin` | أعلى دور داخل المؤسسة المشترية | حسابات مؤسسته وحوكمتها، **قراءة** اشتراكه وفواتيره كاملة، و**طلب** تجديد أو ترقية — لا تنفيذها |
| `manager` / `operator` / `viewer` | موظفو المؤسسة | العمل التشغيلي بحسب أدوارهم |

`owner` يعلو كل حارس دور. وحسابه محميّ في الشيفرة: لا يُخفَّض ولا يُعلَّق من شاشة
الحسابات، ولا يُصنع مالكٌ ثانٍ من نموذج ويب.

### كيف يصير المالك مالكاً

بالترتيب، عند الإقلاع وعند التهيئة الأولى معاً (`ensureOwnerAccount`):

1. يوجد مالك؟ لا شيء يُفعل.
2. `NAHJ_OWNER_EMAIL` يطابق حساباً؟ يُرقّى — وهذه الطريقة الصريحة لمن ينشر.
3. وإلا يُرقّى **أقدم حساب**، وهو حساب من هيّأ النظام.

> من هيّأ النظام يصير مالكه في اللحظة نفسها، لا عند إعادة التشغيل التالية.

---

## 2. الباقات

كتالوج قابل للتحرير كاملاً من شاشة المالك. المزروع افتراضياً: `trial`, `starter`,
`growth`, `enterprise`, `sovereign` — وهي **نقطة بداية لا التزام تجاري**؛ الزرع لا
يدهس تسعيراً عدّله المالك.

كل باقة تحمل:

- **سعراً** لكل دورة (شهري / ربعي / سنوي)، ورسوم تأسيس، وسعر مقعد إضافي شهرياً.
- **حدوداً**: مقاعد، مهارات، حالات عمل شهرياً، موصلات، خوادم MCP، نداءات ذكاء،
  مدّة الاحتفاظ بسجلّ التدقيق، و**سقف الاستقلالية** (L0–L6).
- **مزايا** تُفتح وتُغلق: Teach، محرّك الظل، MCP، موصلات خارجية، واجهة برمجية،
  دخول موحّد، سياسات مخصّصة، علامة المؤسسة، نشر داخلي، دعم بأولوية.

`null` في أي حدّ تعني بلا سقف. وما لا يُذكر عند إنشاء باقة يأخذ **الحدّ الافتراضي
لا اللانهاية** — فتحُ كل شيء بالسكوت أسوأ افتراض ممكن في طبقة ترخيص.

الباقة تُؤرشف ولا تُحذف: الحذف يكسر كل فاتورة صادرة عليها. وباقة عليها اشتراك قائم
لا تُؤرشف إطلاقاً.

---

## 3. دورة حياة الاشتراك

```
trialing → active → (past_due | grace) → suspended
              ↓
           canceled → expired
```

الحالة **تُشتق ولا تُخزَّن**: هي دالةٌ من (نهاية الدورة، الفواتير غير المسدَّدة،
مهلة السماح، الآن). خادمٌ توقّف أسبوعاً ثم عاد يُعطي الجواب الصحيح فوراً، ولا يوجد
"اشتراك عالق على نشِط" لأن مهمة مجدولة فشلت بصمت.

- **`past_due`** — انتهت المدة وعليه مستحق، وما زال داخل المهلة. قابلة للحلّ بدفعة.
- **`grace`** — انتهت المدة ولم يُجدَّد، بلا مستحق. رسالتها مختلفة عمداً.
- **`suspended`** — نفدت المهلة.

### ما يحدث عند التجميد

**الكتابة تتوقف. القراءة لا.** المؤسسة تبقى ترى بياناتها وسجلّ تدقيقها وفواتيرها
كاملة وقابلة للتصدير. حجب البيانات عمّن دفع مقابل جمعها ابتزازٌ لا سياسة تحصيل.

الردّ `402` لا `403`: الأولى تقول «ادفع»، والثانية تقول «لا صلاحية لك» — وهما
مشكلتان مختلفتان تماماً لمن يقرأ الرسالة. والمالك لا يُحجب عن نظامه، وإلا لم يستطع
تجديد الاشتراك الذي حجبه.

### التجديد

`runBillingCycle()` يعمل كل ساعة، و**معاد التنفيذ بلا ضرر**: تشغيله عشر مرات في
الدقيقة يُنتج ما يُنتجه تشغيلٌ واحد. يُجدّد عند الانقضاء إن كان التجديد التلقائي
مفعّلاً، ويُعلّم الفواتير المتأخرة، ويُنفّذ أي انتقال باقة مجدول.

- التجديد يلتقط من حيث انتهت الدورة، فلا تضيع أيام على المؤسسة.
- لكن اشتراكاً مضى على انتهائه **أكثر من دورة كاملة** يُجدَّد من اليوم: وإلا
  أصدرنا فاتورة عن شهور لم تُقدَّم فيها خدمة.
- والتمديد اليدوي ينطلق من اليوم حين تكون المدة قد مضت — تمديد «ثلاثين يوماً»
  يجب أن يمنح ثلاثين يوماً قادمة، لا أن ينتهي قبل أمس.

### تغيير الباقة

- **فوراً**: يُفوتر الفرق **للمتبقّي من الدورة وحده**. فوترة دورة كاملة عند ترقية
  في يومها الأخير مطالبةٌ بمال مقابل خدمة قُدّمت أصلاً.
- **عند نهاية الدورة**: يُسجَّل ويُنفَّذ عند التجديد القادم.
- **التخفيض لا يُعيد نقداً** — يُقيَّد رصيداً يُخصم من التجديد القادم.

---

## 4. المال

كل مبلغ يُخزَّن **بالوحدة الصغرى كعدد صحيح** (فلس للدينار الكويتي، وهو ثلاث منازل
لا اثنتان). الكسور العشرية في المال تُنتج فروقاً لا تُسوّى في فاتورة. والقيم الكسرية
تُرفض بدل أن تُقرَّب بصمت.

- الخصم على المجموع الفرعي، ثم **الضريبة على ما بعد الخصم** — عكسه يُنتج ضريبةً على
  مبلغ لم يُحصَّل. (الكويت بلا ضريبة قيمة مضافة اليوم، والحقل جاهز ليومها.)
- الخصم والضريبة بالنقاط الأساسية (`1000` = 10%)، تجنّباً لكسور النسب.
- أرقام الفواتير متسلسلة لكل سنة (`NAHJ-2026-0001`)، مشتقّة من أعلى رقم قائم لا من
  عدّاد منفصل — فلا ينزلق التسلسل عند استعادة نسخة احتياطية.
- فاتورة بصفر تُولد مسدَّدة، فلا تعلّق اشتراكاً مجانياً.

### الدفع

**نهج لا يستضيف بوابة دفع ولا يلمس بطاقة.** الدفع يقع خارج المنصة (تحويل بنكي،
KNET، شيك)، وما يُسجَّل هنا هو *إثبات* الدفع بمرجعه. ادّعاء بوابة دفع غير موجودة
أسوأ من عدمها.

---

## 5. الحدود المفروضة فعلاً

ليست أرقاماً على شاشة — الخادم يمنع:

| الحدّ | أين يُفرض |
|---|---|
| المقاعد | `adminCreateAccount` يرفض الحساب بعد الحدّ (`402 PLAN_LIMIT`) |
| سقف الاستقلالية | `POST /skills/:id/promote` يرفض ما فوق سقف الباقة (`402 PLAN_AUTONOMY_CEILING`) |
| الكتابة عند التجميد | `enforceSubscription` على جذر موجّه الـAPI |

القياس مشتقّ من النظام لا مُصرَّح به: المقاعد من عدد الحسابات، والمهارات من عقل
المؤسسة، ونداءات الذكاء تُعدّ **عند النداء الفعلي** — فارتداد المحرّك إلى المنطق
الحتمي لا يُحتسب على حصّة المؤسسة.

---

## 6. المسارات

### ما تراه المؤسسة
```
GET  /api/billing/subscription   اللقطة الكاملة: المدّة، الباقة، الاستهلاك، الفواتير، الدفعات
GET  /api/billing/plans          الباقات المعروضة
POST /api/billing/request        طلب تجديد/ترقية/مقاعد/استفسار — يُسجَّل باسم صاحبه
```

### ما يملكه المالك (`owner` فقط، ومحجوب عن البيئة التجريبية)
```
GET   /api/billing/owner/overview
POST  /api/billing/owner/plans                       حفظ باقة
POST  /api/billing/owner/plans/:code/archive|restore
POST  /api/billing/owner/subscription/start
PATCH /api/billing/owner/subscription                شروط: دورة، نهاية، مهلة، خصم، ضريبة، مقاعد
POST  /api/billing/owner/subscription/renew          تجديد (بفاتورة أو بدونها)
POST  /api/billing/owner/subscription/extend         تمديد بلا فاتورة، بسبب يُحفظ
POST  /api/billing/owner/subscription/change-plan
POST  /api/billing/owner/subscription/cancel|resume
POST  /api/billing/owner/subscription/terminate      يحتاج confirm="TERMINATE"
POST  /api/billing/owner/invoices                    إصدار فاتورة ببنودها
POST  /api/billing/owner/invoices/:id/void
POST  /api/billing/owner/payments                    تسجيل دفعة
```

كل إجراء يترك أثراً في `billing_events` باسم من نفّذه وتاريخه. عملية مالية بلا أثر
لا تُراجَع، ولا تُسوّى عند الخلاف.

---

## 7. الجداول

`billing_plans` · `billing_subscription` · `billing_invoices` · `billing_payments` ·
`billing_events` · `billing_usage` — في نفس ملف SQLite الذي يحمل الحالة التشغيلية،
فلا نسخة احتياطية تحفظ العمل وتفقد عقده.

---

## 8. متغيّرات البيئة

| المتغيّر | الأثر |
|---|---|
| `NAHJ_OWNER_EMAIL` | يُرقّى صاحبه إلى مالك المنصة. غيابه ⇒ أقدم حساب. |
| `NAHJ_TRIAL_DAYS` | طول التجربة التلقائية لنشرٍ بلا اشتراك (افتراضياً 30). |

نشرٌ جديد بلا اشتراك يبدأ بتجربة ثلاثين يوماً بدل أن يُقفل على نفسه أو يُفتح بلا
حدّ. والمالك يستبدلها بعقد حقيقي من شاشته.


## التحصيل الإلكتروني

الفاتورة تُسدَّد من شاشة الاشتراك مباشرة: تضغط المؤسسة «ادفع» فتُفتح صفحة
المزوّد (كي نت أو بطاقة)، ويُسجَّل السداد على الفاتورة تلقائياً **بعد أن يؤكّده
المزوّد نفسه** — لا بعد أن يزعمه إشعار.

ويصل إثبات الدفع إلى الدفتر نفسه من طريقين، عمداً:

1. **يد المالك** — بعد تحويل بنكي أو شيك، من لوحة المالك ← تسجيل دفعة.
2. **البوابة** — بعد تأكيد المزوّد، بمرجعه، وبوسيلته (كي نت/بطاقة).

فلا دفتران يختلفان، ولا رقمان لنفس الفاتورة. وبلا بوابةٍ مضبوطة لا يظهر زرّ
دفع أصلاً ويُقال على الشاشة إن السداد يتم خارج المنصة.

الربط والتشغيل والانتقال إلى البيئة الحيّة: `PAYMENTS.md`.
